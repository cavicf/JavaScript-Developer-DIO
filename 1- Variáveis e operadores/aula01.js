console.log("Imprimir algo") // Console log é usado para imprimir coisas no terminal, permitindo que vejamos os nossos resultados

// Variaveis são usadas para armazenar valores em memoria, como se fossem "caixinhas" que guardam alguma coisa 

let variavel1 = 10; // o let é usado para declarar variaveis que podem mudar seu valor com o tempo
const pi = 3.14; // Já o const é usado pra declarar variaveis que não mudam de valor, ou seja, são constantes

console.log(variavel1); // Aqui passamos a variavel como parametro para o console.log, assim imprimindo o valor dela no terminal

variavel1 = 20; // Aqui estamos mudando o valor da variavel1 para 20

console.log(variavel1); // Agora ao imprimir a variavel1 novamente, veremos que seu valor mudou para 20


// Operadores são as "ferramentas" que usamos para realizar operações com os valores armazenados nas variaveis
// o mais básico é o operador de atribuição (=), que usamos para atribuir valores às variaveis
//Temos também operadores matemáticos como + (adição), - (subtração), * (multiplicação) e / (divisão)
let soma = 10 + 10; 
console.log(soma)

let subtração = 10 - 5;
console.log(subtração)

let multiplicacao = 2 * 4;
console.log(multiplicacao)

let divisao = 8 / 4;
console.log(divisao)

